# Positive Grammar Syntax local semantic engine
# Copy to .env for local development. Do not commit secrets.

PGS_OLLAMA_URL=http://127.0.0.1:11434
PGS_OLLAMA_MODEL=qwen3.8:latest

# Cloud fallback remains disabled until explicitly configured in a later stage.
PGS_CLOUD_FALLBACK=false
