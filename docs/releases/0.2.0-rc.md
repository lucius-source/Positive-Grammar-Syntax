# PGS 0.2.0 Internal Release-Candidate Record

**Evaluation date:** 2026-09-17

**Evaluated commit:** `ed46cdbf1a178a94cb7cd3a61dfae4d766d83bf7`

**Package version:** `0.2.0`

**JSON schema:** `pgs.output.v1`

## Deterministic gate

`npm run release:check` passed on the evaluated source tree:

- 187/187 automated tests;
- compiled package and public consumer verification;
- 19/19 adversarial fidelity cases; and
- 350/350 seeded fuzz cases across 14 invariant families.

## Local semantic gate

The Ollama preflight and complete canonical evaluation passed using:

- provider: `local`;
- engine: `ollama:qwen3.8:latest`;
- endpoint: `http://127.0.0.1:11434`;
- canonical results: 50/50 passed, 0 failed; and
- cloud fallback: disabled.

The gate was run once in human-readable mode and repeated in JSON mode to persist detailed local evidence. The JSON run contains 50 result records.

## Evidence artifact

- Local path: `artifacts/live/local-evaluation.json`
- Size: 367508 bytes
- SHA-256: `3fa49af2fbe72c5bdd6f59b841ea67a2902c804cb87bf9382b1164c7e1646ac9`

The artifact directory remains ignored because evaluation output is derived and can contain complete source cases and model determinations. The checksum binds this record to the exact local artifact. The manual self-hosted workflow remains the mechanism for retaining an uploaded copy when a centrally persisted record is required.

## Decision

The `0.2.0` candidate satisfies the deterministic and local-model gates defined in `config/release-contract.json`. It is eligible for an internal `v0.2.0` tag. This does not authorize registry publication or changing package visibility.
